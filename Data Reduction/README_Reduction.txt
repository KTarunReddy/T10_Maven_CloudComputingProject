a.) Run Category_Reduction.py and Company_Reduction.py scripts on the offers.csv and transactions.csv files.
b.) reduced_company and reduced_category datasets are generated.
c.) The generated datasets are merged by removing the duplicates using sort -u reduced_category reduced_company > reducedFile. 
d.) The reduced file is utilized for further analysis in the project.  
